// TODO
// Intercepts all calls not caught by configured origin path patterns (/site, /api, /auth etc).
// Rewrites calls to s3 so that they all resolve to the static 404 page in the root of the content s3 bucket.
