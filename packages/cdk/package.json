{
  "name": "apis",
  "version": "0.1.0",
  "bin": {
    "apis": "bin/apis.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "lint": "eslint  --max-warnings 0 --fix --config ../../eslint.config.mjs .",
    "synth": "cdk synth --context stackName=clinical-tracker-ui",
    "check-licenses": "license-checker --failOn GPL --failOn LGPL --start ../.."
  },
  "devDependencies": {},
  "dependencies": {
    "aws-cdk-lib": "^2.159.1",
    "cdk-nag": "^2.28.195",
    "constructs": "^10.0.0",
    "source-map-support": "^0.5.21"
  }
}
