import React from "react";

export const metadata = {
  title: 'EPS Your selected role'
}
export default function ClientLayout({
    children,
}: {
    children: React.ReactNode
}) {
  return children
}
