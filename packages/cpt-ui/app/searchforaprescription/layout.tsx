import React from "react";

export const metadata = {
  title: 'EPS Prescription Search'
}
export default function ClientLayout({
    children,
}: {
    children: React.ReactNode
}) {
  return children
}
